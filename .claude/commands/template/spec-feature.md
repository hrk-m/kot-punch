# Spec: {機能名}

## 概要

{機能の目的・提供する価値を 1〜2 文で記述する}

---

## 前提条件

{機能が正常に動作するために必要な状態・設定・依存を列挙する}

- {例: グローバル設定に `kingOfTimeUrl` / `tokenKey` / `token` が設定済みであること}
- {例: Stream Deck プラグインが起動していること}

---

## 入力・出力

| 項目 | 型 | 説明 |
|------|----|------|
| 入力: {パラメータ名} | {型} | {説明} |
| 出力: {戻り値} | {型} | {説明} |

---

## 主フロー（正常系）

```
{トリガー（例: onKeyUp）}
  │
  ├─ {ステップ 1}
  ├─ {ステップ 2}
  └─ {最終結果}
```

---

## 異常系・エラー処理

| エラー条件 | 対応 |
|---|---|
| {例: 必須設定が未入力} | {例: showAlert() を表示して処理を中断} |
| {例: Puppeteer 起動失敗} | {例: showErrorImage() でエラー表示} |

---

## 状態遷移

{UI やアクションが持つ状態と、その遷移条件を記述する}

| 状態 | 説明 | 遷移条件 |
|------|------|---------|
| {例: State 0} | {例: 未打刻（通常アイコン）} | {例: 初期状態 / エラー後リセット} |
| {例: State 1} | {例: 打刻済み（チェックマークアイコン）} | {例: 打刻成功後} |

---

## 制約・非機能要件

- {例: 連打防止: `_isProcessing` フラグで処理中の重複実行を防ぐ}
- {例: dryRun モード: `dryRun=true` の場合は submit をスキップして動作確認のみ行う}

---

## エッジケース・境界条件

| ケース | 挙動 |
|--------|------|
| {例: State 1 でボタンを押した場合} | {例: State 0 にリセットして処理を抜ける} |
| {例: 処理中に再度ボタンを押した場合} | {例: _isProcessing フラグにより即 return} |

---

## 依存関係

| 依存先 | 用途 |
|--------|------|
| {例: `lib/puppeteer.ts`} | {例: Puppeteer 起動・打刻操作} |
| {例: `lib/settings.ts`} | {例: グローバル設定の取得・バリデーション} |
| {例: KING OF TIME（外部）} | {例: 打刻対象のウェブサービス} |
